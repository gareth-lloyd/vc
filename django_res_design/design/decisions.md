# Decisions Log

The living log of design decisions surfaced by the workflow audit
(`../legacy/workflows/**`) against the design docs. Keyed by **what we decided**,
not by **what legacy did** — [`departures.md`](departures.md) is the
per-legacy-table mapping; this file is "why doesn't the design do X?" / "where
did this come from?". This is the canonical home for decision rationale; frozen
specs under `backend/` and `product/` may restate a decision but this file wins.

Each row carries a status:

- **live** — the decision is realised in the current design docs.
- **deferred** — recognised, intentionally postponed past v1.
- **dropped** — considered, rejected for the reasons noted.

Status is point-in-time; flip rows as the design evolves.

## Live decisions

| Decision | Workflow IDs / loci | Docs touched | Rationale |
|---|---|---|---|
| **Inclusions un-bundled from rates into first-class `PropertyService` (GAP-037, owner Loom 2026-06-17).** Legacy `RatePlan.inclusion` free-text is promoted to a property-scoped, date-ranged `properties.PropertyService` (guest-facing `copy` + internal `notes`, absolute `applies_from/to` band, `sort_order`, `is_active`) on its own **Services** tab beside Pricing. Services are **informational, never priced** (cost is baked into the rate) — so they do **not** touch `pricing.Extra` (the priced-add-on home) and add **no fourth** inclusion concept: `PropertyService` (included, date-ranged prose) / `Extra` (priced add-ons) / `Feature(INCLUDED_SERVICE)` (amenity tags). The engine derives `breakdown["inclusion"]` from active services whose band overlaps the stay (projection maps future-year stays back to the anchor year); `seed_inclusions` still seeds the operator-editable `QuotationLine.inclusions`. `RatePlan.inclusion` is **dropped**; `RatePlan.notes` is **kept**. **Supersedes** the "inclusions drive how many seasons" row below — rates, tier labels, and inclusions are three independent overlays on one calendar. | `todo/done/gap-037-services-as-separate-entity-and-tab.md`; Q-022 (tiers), GAP-018 (guest copy); owner Loom 2026-06-17 (pricing walkthrough 0:58–1:30) | `02-properties.md`, `04-pricing.md`, `properties/models/services.py`, `pricing/engine.py`, `frontend/.../properties/tabs/ServicesTab.tsx` | A villa with flat rates but a summer-only chef was forced into two identical-rate seasons purely to express the chef; un-bundling makes it one `RatePlan` + one service band. Informational-only keeps the pricing-total path untouched and avoids the "is this extra free?" ambiguity that overloading `Extra` would create. |
| **Enquiry stage vocabulary renamed to the operator dashboard wording** (2026-06-18): `EnquiryStatus` values `contacted`/`quoted`/`lost` become `progressing`/`quote_sent`/`dead` (`new`/`converted` unchanged); the enum members, DB rows (`Enquiry.status` + `EnquiryEvent.from_status`/`to_status`, rewritten by a `RunPython` migration), the API, and the React `enquiryStatusSchema`/i18n labels all move together. Event *kinds* (`EnquiryEventKind`) are a separate vocabulary and are untouched. This is the shared stage foundation GAP-038/039 build on ("land the enum once and reuse"). | `todo/gap-038` (stages + conversion metric), `gap-039` (dashboard enrichment), `gap-005` (quotation-flow parity) | `05-reservations.md`, `reservations/enums.py`, `frontend/.../enquiries/schemas.ts` | The Ben/owner mockup names the pipeline columns `New Enquiry / Progressing / Quote Sent / Follow-up`; the code, DB, and dashboard previously disagreed on the wording. One vocabulary across the stack lets the list, Kanban, and conversion metric share stage names without a translation layer. |
| **Unified `Person` identity model** (2026-06-18): one `Person` absorbs `accounts.Contact` + `reservations.Guest`; fine-grained capacity (traveller/agent/owner/manager/…) is a role/relationship, **never a capacity column** (the shipped model carries only a coarse `kind` = `CUSTOMER`/`CONTACT` demand-vs-supply partition); a real `Organisation` entity replaces free-text `company`; **`User` stays first-class** as `OneToOne → Person`. Directories (Clients/Villa Contacts/Companies) are filtered views. **Overturns** the earlier "Guest kept distinct from Contact" call. | `todo/gap-045` (unify), `gap-046` (Organisation + agent), `gap-047` (Clients), `gap-048` (Villa Contacts/roles); 2026-06-17 owner Contacts review | `people-model-cleanup.md` (banner), `01-accounts.md`, `05-reservations.md` | Identity ≠ role ≠ login: the same human is owner + traveller + agent across time. Fragmenting identity across two tables duplicated people and stranded "agent" (a demand-side role) in the supply-side `Contact` bag. One identity + roles-as-relationships fits the domain and the owner's directory UI; resolves the deferred "Guest channel richness" item. |
| **Agents fold into the Clients directory, not a separate Agents page** (owner Loom 2026-06-29). "An agent is effectively a client … should sit in the same list, just a different category" — overruling Ben's separate-Agents-page mockup (§2.13). Clients = `kind=CUSTOMER` + agent-capacity people behind a **direct/agent** filter; B2B agency *companies* get their own **Companies** section (`Organisation`, GAP-046). | `todo/gap-047` (Clients incl. agents), `gap-046` (Companies); 2026-06-29 owner Loom | `01-accounts.md`, `mock_up_analysis/01-new-res-system.md` (§2.12/§2.13) | One identity, capacity-scoped views. The owner's mental model is two human lists (Clients incl. agents, Suppliers) + a Companies list — not a third Agents page. |
| **"Contacts" directory renamed "Suppliers" and capacity-scoped** (owner Loom 2026-06-29). The operator-side directory shows only owner/villa-manager/villa-admin/management-company people; customers + agents move to Clients. Three directory views over one `Person`: Clients / Suppliers / Companies. **Naming collision flagged:** "Suppliers" also names the concierge in-resort vendor concept (Q-007) — resolve the labels. | `todo/gap-048`, `q-007`; mockup §2.14 (page already labelled "Suppliers") | `01-accounts.md`, `mock_up_analysis/01-new-res-system.md` §2.14 | Matches the mockup label and the owner's directory split; keeps the operator list free of the much larger customer population. |
| **Concierge suppliers are contacts, not a standalone `Supplier` entity** (Q-007, 2026-07-02). When the concierge module (workflow 9) is built, `ConciergeLineItem` references a supplier as a nullable FK to the existing directory (`Person`/`Organisation` — GAP-045/046/048 already give suppliers typed identity, company form, and property roles); internal `supplier_cost` lives **on the line** (margin reporting), never on the supplier. No contracts / payment-terms / insurance-docs model. Concierge role vocabulary, the optional `notify_on_concierge_request` flag, and the "Suppliers" label collision (directory vs concierge vendors, flagged in the GAP-048 row above) are settled when the module is picked up. | `todo/done/q-007-concierge-supplier-directory.md`; `product-design/03-workflows.md` §9 | none yet (concierge module unbuilt) | The workflow spec needs only "supplier (optional, from contact directory)" + per-line cost, and legacy had no supplier entity at all (margin was an operator spreadsheet). A contracts/insurance ERP would be invented scope; the unified-Person work already made contacts rich enough. |
| **2FA enforcement: all staff must enrol; refund execution always requires a fresh TOTP** (Q-008 → GAP-057, 2026-07-02). (1) Enrolment is forced for every `is_staff=True` user via middleware behind a `TFA_ENFORCED` settings flag (prod on; dev/test off) — an unenrolled staff session can reach only the login/enrolment allowlist; self-serve `:disable` is refused while enforced (admin `users/{pk}:reset-2fa` is the escape hatch). Non-staff principals (owner portal / magic link, 501 stubs) are out of scope. (2) `POST /refunds/{id}:execute` demands a valid **single-use** TOTP on every call (`tfa_code`; replay-guarded via `tfa_last_verified_step`) even when the session logged in with 2FA — freshness is the TOTP window itself, no session step-up cache; recovery codes do not work for step-up; `actor=None` system callers exempt; approve/reject/cancel do not step up. Errors: 403 `tfa_stepup_required` / 400 `invalid_tfa_code`. A review of the enrolment middleware also swapped DRF's stock `BasicAuthentication` for `StaffExcludedBasicAuthentication` — Basic auth runs at the view layer *after* the session-only middleware, so an unenrolled staff Basic header would have bypassed both enforcement and the 2FA login (non-staff owner-feed principals keep Basic). | `todo/done/q-008-2fa-enforcement.md`; build plan `todo/done/gap-057-2fa-enforcement-and-refund-stepup.md` | `01-accounts.md` (enforcement policy, middleware, allowlist, `verify_code`, `tfa_last_verified_step`, `StaffExcludedBasicAuthentication`), `07-payments.md` (`:execute` step-up) | The mechanism shipped but nothing forced enrolment, and a hijacked session cookie sufficed to execute a refund. One uniform staff rule beats a goes-stale "touches refunds" predicate (`role` defaults to VIEWER, so role can't gate it); tying step-up freshness to the TOTP window avoids designing a second "recently verified" expiry state. |
| **comms stays top-of-spine and is strictly pull-only** (Q-017 → GAP-058, 2026-07-02). Nothing below comms ever imports it; comms consumes the domain exactly two ways — signal receivers in `comms/signals.py` (events: booking transitions, quotation sent, payment succeeded/failed, …) and its own beat sweeps in `comms/tasks.py` that read domain models downward (time: payment/SD reminders, stuck-email requeue). All seven blessed `→ comms` back-edges dissolve under the rule: `send_payment_reminders` is a pure notification sweep and moves to `comms/tasks.py` (kills the five `payments.tasks` edges), password reset fires a new `accounts.password_reset_requested` signal (kills the direct `EmailService` call), and comms mounts `bookings/{pk}/emails` from its own URL conf (kills the routing edge). End state: zero `-> comms` lines in `ignore_imports`; a direct call from a domain app fails `lint-imports`, so the per-feature "signal or direct call?" debate is closed structurally. | `todo/done/q-017-comms-direction-signals-vs-spine-position.md`; build plan `todo/gap-058-comms-pull-only-top-of-spine.md`; 2026-06-10 backend review | `00-conventions.md`, `django_res/CLAUDE.md` layers rationale, `pyproject.toml` `ignore_imports` (all updated when GAP-058 lands) | comms is overwhelmingly a *reader* of the domain (signals/recipients/contexts walk reservations, payments, properties) — top-of-spine makes those reads free and pins the sink's true dependency direction; the seven upward exceptions existed only because comms work was built in the wrong app or wired as a direct call, not because the layering was wrong. |
| **Contact `ContactRole` taxonomy reconciled to the legacy/owner five** (owner Loom 2026-06-29): re-introduce `villa_admin` + `management_company`; `management_company` may point at an `Organisation` (GAP-046). The added `housekeeper` / `owners_rep` are kept-or-dropped per a recorded call (default: keep — the owner didn't ask to remove them). | `todo/gap-048`; legacy `VillaRoles` (Owner/Agent/Villa Admin/Villa Manager/Management Company) | `01-accounts.md` (`ContactRole`), `properties/models/contacts.py`, loaders, FE dropdowns | The built enum drifted from the legacy 5 the spec calls it "the direct replacement" for; the owner's §2.14 role dropdown lists exactly those five. |
| **A contact requires a name OR an agency, not both names** (GAP-029, 2026-06-11 owner email). `Person.first_name`/`last_name` are `blank=True` — an agency/company-only contact carries no personal name. The "at least a name or an agency" floor is a serializer `validate()` gate (app-level, mirroring the sibling channel-contactability floor in the same method), **not** a DB `CHECK` — avoids a migration over the audited/anonymized/legacy `Person` rows. Error keyed on `first_name` so the FE renders it inline under the name field, matching the FE refine's `path: ["first_name"]`. FE was already loosened; this closes the FE/BE divergence. | `todo/gap-029`; spun out of GAP-027; unblocked by GAP-045/046 | `01-accounts.md`, `accounts/models/person.py`, `accounts/serializers/contact.py`, `frontend/src/features/contacts/schemas.ts` | The FE `contactWriteInputSchema` already refined to "name OR agency", but the backend model left the names required, 400'ing the company-only / minimal contact the loader and the owner explicitly asked for. |
| **Customer tags are client-only and edited inline (no dialog)** (owner Loom 2026-06-29). The GAP-040 tag set (VIP/Trade/…) is relevant to Clients only — hidden on the Suppliers detail — and the editor moves from a checkbox *dialog* to **inline checkboxes**; VIP/Trade/Repeat also become one-click chip **filters** on the Clients list (Repeat = derived `is_repeat_customer`, not a stored tag). | `todo/gap-053` (chips + inline editor), `gap-040` (tags), `gap-042` (Repeat derivation), `gap-048` (excluded on Suppliers) | `01-accounts.md` (tags section), `mock_up_analysis/01-new-res-system.md` §2.12 | The Loom is explicit: checkboxes not a modal, tags belong to clients, the three flags are first-class list filters. |
| **Contact address + notes are operator-editable; type badges surfaced** (owner Loom 2026-06-29). **Overturns** the GAP-042 "address display-only / country-deferred" call and the read-only notes block — address (incl. country FK) and notes become editable on both the Clients profile and the Suppliers detail; the detail shows derived **type badge(s)** (Customer/Agent/Owner/…) reflecting every capacity the person holds. | `todo/gap-052`; supersedes `gap-042` deferrals | `01-accounts.md`, `mock_up_analysis/01-new-res-system.md` §2.12/§2.14 | The owner asked for editable address + finished/editable notes + a clear indication of contact type(s); GAP-042 had shipped these read-only as an interim. |
| **Per-user SMTP reinstated as `comms.SmtpProfile`** (scope=PERSONAL, encrypted creds) for agent "send as" on quotation emails. Reverses an earlier "dropped as security liability" call. | `workflows/11-integrations/transmission.md`, `workflows/02-administration/user-administration.md` | `10-comms.md`, `01-accounts.md`, `09-departures.md` | Guest replies must land in the agent's inbox; a single shared system SMTP can't deliver that. Encryption-at-rest + admin-only access mitigates the security concern. |
| **`comms` app created in v1** with `EmailTemplate` (versioned, DB-stored, file-seeded), `EmailLog` (persist-first, append-only), and `EmailService.send(...)` (routes via personal or system SMTP). Listens for `booking_transitioned`, `payment_succeeded`, `hold_expired`, etc. | `workflows/11-integrations/email-delivery.md`, `transmission.md`, `flywire-gateway.md` | `10-comms.md`, `06-availability.md`, `07-payments.md`, `00-conventions.md`, `09-departures.md` | Multiple apps need transactional email. A single audited dispatcher beats every app rolling its own `send_mail`. `EmailLog` correlation provides reminder-idempotency without per-row flags. |
| **`RateRule.is_locked` + `RateRule.is_approved`** gates. Bulk recompute / re-import services skip `is_locked`; `PricingEngine.quote()` filters `is_approved=False`. | `workflows/04-pricing/rates.md` (legacy `IsManualUpdate` and `IsApprove`) | `04-pricing.md` | Preserves the legacy operator behaviour of hand-set rates surviving bulk operations, and gates un-reviewed imports out of the engine. |
| **`PricingEngine` precedence = card order** (supersedes the earlier priority/specificity tie-break): cards walk in `(sort_order, pk)` order; the first card with a rule covering night + party wins. No specificity tie-break; in-memory projected duplicates resolve to lowest `pk`. | `workflows/04-pricing/pricing-engine.md`; 2026-06-10 rate-precedence review | `04-pricing.md` | Determinism over flexibility, with one knob instead of two: the UI never exposed `priority`, and a rule-level field the UI can't show is a precedence the operator can't see. |
| **`RateRule.priority` deleted; loader resolves legacy overlaps** (2026-06-10). Within-card overlap is forbidden unconditionally (`raterule_no_overlap` EXCLUDE, no priority arm); `RateCard.sort_order` is the only precedence mechanism. `RateRuleLoader` resolves legacy `VillaSeasonRate` overlaps at load time: boundary trim for checkout-style contiguous bands, then approved rows claim space before unapproved, earliest legacy ID wins within a tier, clip-only (losers shrink to their largest uncovered remainder or drop). Each load is a full replace — purge legacy-loaded rules, reinsert; `--since` ignored with a warning — so re-runs never collide with the previous run's spans under the EXCLUDE constraint. | Legacy `sp_get_quote_weeks_price` (`SELECT TOP 1`, no `ORDER BY`); 24-Apr-2025 prod-dump overlap analysis | `04-pricing.md`, `09-departures.md`, `data_migration/CUTOVER.md` | Legacy had no precedence concept — its overlap winners were formally arbitrary — and both `priority` and the old per-priority EXCLUDE were rebuild inventions that trapped UI-created rules (priority 0) into silently losing every overlap against migrated rows (stamped `legacy_id % 65535`). Prod overlaps are noise (735 true pairs, 379 with differing prices), so they are resolved once at load, not at every quote. |
| **Changeover auto-shift reinstated, property-only, always-shift** (GAP-007). A non-conforming arrival is nudged forward to the next valid changeover day before pricing. Changeover resolves from the **property only** (`ChangeoverService`: `ChangeOverRule` window → `PropertySettings`/group chain; `any` = no shift) — the per-card `RateCard.changeover_weekday` field was retired (a rebuild invention, null everywhere). Night count is preserved by shifting `date_to` equally; `Quote.changeover_shifted_from` surfaces the original date for a "we moved your dates" note, and the shifted dates are persisted onto the line, hold, and booking. The arrival is **never rejected** — the hard-reject gate (`validate_arrival`) and its `allow_changeover_override` escape were removed entirely (breaking v1 API change). | 2026-06-02 pricing audit; legacy `ResService.cs:2028-2041` | `04-pricing.md`, `09-departures.md`, `10-decisions.md` | The rebuild had hard-rejected off-weekday arrivals — an unflagged regression vs legacy's silent nudge — and carried two contradictory changeover sources. Collapsed to one: property-level, always shift + surface, never reject. |
| **No-rate-for-night fallback reinstated as opt-in `RatePlan.fallback_nightly`** (GAP-008). When no `RateRule` covers a night, the engine prices it at `fallback_nightly` via a synthetic `QuoteLine` (`rule_id`/`card_id` `None`); `NULL` keeps today's hard `NoRateAvailable`. `OutOfRange` still raises `PartyOutOfRange`; an all-fallback stay skips card validation. | 2026-06-02 pricing audit; legacy `ResService.cs:2150-2160` | `04-pricing.md`, `09-departures.md` | Legacy quietly priced gaps at `SettingNightlyPrice × 7`, so sparse-rate villas still quoted. The rebuild restores that leniency as an explicit, currency-scoped opt-in rather than a silent property-price echo. Answers the engine half of Q-013. |
| **Incomplete pricing ⇒ flag + manual quote; the villa is never hidden** (Q-013). A bulk-quote entry the engine can't price (`no_rate_available` — missing rules or POA) is promoted into the builder's main results list flagged "Incomplete pricing — manual quote" with an "Add manually" affordance; the line stages `is_manual` (operator types total + reason, server-validated), and the entry carries `hero_image_url` + the property's resolved `currency_code` so the operator sees the currency the manual total saves in. Other error codes stay collapsed/unselectable. Accepted: POA shares the flag (tooltip surfaces `error_detail`); manual lines bypass the changeover auto-shift. | `workflows/08-quotation/*`; legacy `RateLookup.razor:400-417` (NO RATE rows stay selectable), `Booking.razor:122` (operator-typed price), `QuoteGenerator.razor:833/887` (`IsManual` villas) | `todo/q-013-rate-card-incomplete-pricing.md`, `todo/gap-005-quotation-flow-parity.md` #10, `todo/gap-013-quote-builder-ux-feedback-loops.md` | Legacy parity for a customer-facing flow: hiding an unpriceable villa loses revenue legacy captured by letting the operator quote it manually. |
| **Customer-facing reference format matches legacy `QVC`/`VC`, booking number carried forward** (GAP-006). `Quotation` owns a sequence-backed `number` (`quotation_number_seq`, `unique`); its reference renders `QVC{number}`. The booking reference is **derived from the same number** (`QVC184` → `VC184`), not an independent sequence — matching legacy `Constant.QUOTATION_NO_PREFIX`/`BOOKING_NO_PREFIX`. Prefixes default in `core.refs` and override via `SystemSettings.settings` (`quotation_no_prefix`/`booking_no_prefix`). A numberless quotation (synthesised/interim) yields a `VC-TMP-…` sentinel, never a bare `VC{int}`. Loaders preserve exact legacy digits; the synthesised per-booking quotation keeps `number` NULL so it never collides with the real quotation that owns that number. Scope of the *carry-forward number* is Quotation + Booking only. Enquiry/Payment/Refund/SecurityDeposit later moved off `generate_reference` to their own `db_default`-backed sequences (BUG-007, `{prefix}-{year}-{nextval}`) — same DB-allocation principle, independent series, no carry-forward. No backfill of seeded rows (reseed via `seed_dev`); post-import, bump the quotation sequence via `setval` (see `data_migration/CUTOVER.md` §4c). The BUG-007 series need no `setval` sync — their format is disjoint from the legacy-imported references. | legacy `Enums.cs:282-283`, `ResService.cs:3328` | `01-domain-model.md`, `05-reservations.md`, `02-frontend-design.md`, `03-workflows.md`, `05-improvements-over-original.md`, `mock_up_analysis/02-client-portal.md`, `data_migration/CUTOVER.md` | Reference numbers are customer-facing, so must match what guests/agents saw in `ResSystem`. The rebuild had emitted `Q-2026-NNNNNN`/`B-2026-NNNNNN`, matching neither legacy nor the carry-forward relationship operators rely on to eyeball a quote↔booking pair. |
| **`Payment.purpose=CONCIERGE`** + `Payment.concierge_item` FK + `ConciergeService.request_payment(items)`. Excluded from `Booking.outstanding_balance()` schedule maths. | `workflows/09-booking/booking-concierge.md` | `07-payments.md` | Ad-hoc concierge invoices don't fit the 3-tier deposit/balance/security schedule but need first-class payment-history visibility. |
| **`PropertyFinance.cancellation_fee_amount` / `cancellation_fee_percent` / `cancellation_window_days`** + `GroupFinance` mirror. `RefundService.from_cancellation(booking, reason)` computes the refund. | `workflows/09-booking/booking-cancellation.md`, `booking-modification.md` | `03-finance-config.md`, `07-payments.md`, `09-departures.md` | Legacy had no refund workflow at all; cancellation was a data-delete. The new cancellation path needs a configurable fee + a `Refund` row sized as `paid_total − fee`. |
| **`PropertySettings.requires_enquiry_first`** (nullable bool, inherits from group). Public site hides direct-book affordance when True. | `workflows/06-availability/availability-check.md` (legacy status code 20 "Available – Enquire") | `02-properties.md`, `09-departures.md` | Restores a real UX affordance without inflating the 3-value `Property.status` enum (`DRAFT` / `ACTIVE` / `ARCHIVED`). |
| **Hold auto-expiry enabled from day one.** Celery beat task `expire_holds` (every 1 min) sets `released_at`, emits `hold_expired` signal; `comms` sends an email to the creating agent. | `workflows/06-availability/holds.md` (legacy `[DISABLED]` scheduler), `[NEW] AVAILABILITY.HOLD.EXPIRE_NOTIFY` | `06-availability.md`, `00-conventions.md`, `10-comms.md` | Legacy required manual cleanup; the Django port closes that gap on day one. |
| **All Celery beat tasks enabled in v1**: `expire_holds`, `escalate_pending_owner_approvals`, `send_payment_reminders`, `process_sd_refunds`, `auto_check_out`, `cleanup_orphan_images`, `zoho_reconciliation`. Idempotency declared per task. **Status (2026-06-09):** Celery is wired and the *implemented* tasks are beat-scheduled — `expire_holds`, `ingest_ical_feeds`, `escalate_pending_owner_approvals`, `auto_check_out`, `send_payment_reminders`, plus a `requeue_stuck_emails` durability sweep. The not-yet-implemented tasks (`process_sd_refunds`, the `integrations.*` sync tasks ≈ `zoho_reconciliation`, and `cleanup_orphan_images`) are decorated but **deliberately left out of the schedule** until their bodies exist — beat would otherwise raise every tick. Each gets scheduled in its own implementation ticket. | `workflows/12-automation/scheduler-jobs.md` | `00-conventions.md` | Legacy `SchedullerJob` was checked in but `[DISABLED]`; payment reminders, SD refunds, and hold-expiry never ran. Django port runs them with retry + DLQ. |
| **`SecurityDeposit` and `Refund` as first-class workflow models** (not just Payment rows). State machines on the workflow row; gateway-transaction audit on spawned `Payment(purpose=…)` rows. Separation-of-duties on `Refund` (requester ≠ approver). | `workflows/10-payment/payment-preauth.md`, `payment-collection.md`; reconciliation issues #24, #25 | `07-payments.md`, `09-departures.md` | Legacy had no refund table at all and a flat `IsSDPaid` flag with no pre-auth lifecycle. The new shape models both properly. |
| **Generic `SyncRecord`** + `SyncRun` + `SyncIssue` for cross-provider sync. Idempotent on `(provider, external_id)`. Celery autoretry with exponential backoff; max 6 attempts; `SyncIssue` on exhaustion. | `workflows/11-integrations/zoho-sync.md`, `enquiry-intake.md` (legacy `[CORRECTNESS]` silent loss on Zoho outage) | `08-integrations.md` | Replaces scattered `ZohoId` / `SyncId` columns with one observability point. Fixes legacy fire-and-forget pattern that lost data silently. |
| **No `SoftDeleteModel`.** Lifecycle is always an explicit visible signal: `status` enum, `is_active`, dated timestamps, or hard delete + `AuditLog`. | `00-conventions.md` "Lifecycle, not soft delete" | every doc | Soft-delete tombstones rot; explicit lifecycle scales. |
| **Range-query availability** backed by Postgres `EXCLUDE USING gist` constraints; no daily-row grid. | `workflows/06-availability/availability-check.md`, `booking-status-transitions.md` (legacy `[CORRECTNESS]` two-write hazard) | `06-availability.md` | DB-enforced no-overlap is stronger than the legacy SP pair `sp_villaAvailability` + `SP_SAVE_BOOKING_INFO` — and atomic. |
| **One flat `PropertyFinance`** + `GroupFinance` mirror with prefixed fields (`commission_*`, `tax_*`, `bank_*`, `deposit_*`, `interim_*`, `security_deposit_*`, `cancellation_*`). MVP staff roles gate the whole form; per-concern permissions are a post-v1 refactor. | reconciliation issue #36 | `03-finance-config.md` | An earlier draft used 5 OneToOne children for granular permissions that MVP roles don't need. Collapsed to keep admin/serializer/resolver surface flat. |
| **Per-module Zoho serializers + response capture** handled by the generic `SyncClient` hierarchy; module specifics live in subclasses, not in a separate per-module table. | `workflows/11-integrations/zoho-sync.md` | `08-integrations.md` | Implementation detail handled by the existing client abstraction — no additional spec needed. |
| **Webhook secrets in env / Vault**, HMAC verified on raw body bytes. Persist-first `WebhookDelivery` with idempotent `(provider, event_id)`. | `workflows/10-payment/payment-collection.md`, `flywire-gateway.md`; security-debt items #1, #3, #4 | `07-payments.md`, `08-integrations.md` | Closes the legacy debts: hardcoded API key in source, unsigned webhooks, no idempotency check. |
| **Cancel-and-rebook for post-deposit modifications.** Pre-deposit modifications mutate the booking in place; post-deposit changes force `booking.cancel(reason='guest_modification')` + new Booking creation. State machine stays at 11 states. | `workflows/09-booking/booking-modification.md` | `05-reservations.md`, `06-availability.md` | Avoids inventing a `MODIFICATION_PENDING` state; refund/repricing trail is uniform via cancel+rebook. |
| **Flywire is the v1 payment gateway.** No multi-provider abstraction layer; the `Payment.provider` enum (`FLYWIRE`, `MANUAL_BANK_TRANSFER`) stays extensible so a second provider can be added later without a migration, but no Stripe / other-gateway code paths are built. | `workflows/11-integrations/flywire-gateway.md`, `workflows/10-payment/payment-collection.md`, `workflows/10-payment/payment-preauth.md` | `07-payments.md`, `08-integrations.md`, `product-design/00-overview.md`, `product-design/01-domain-model.md`, `product-design/04-rest-api-surface.md`, `product-design/06-verification.md` | Continuity with the legacy gateway integration (webhook contract, tokenisation flow, refund window semantics). Switching processors at rebuild time is a separate project; not in scope for v1. |
| **Per-booking Communications tab on Booking Detail.** | `product-design/02-frontend-design.md §3.8` | `product-design/02-frontend-design.md`, `product-design/05-improvements-over-original.md`, `10-comms.md` | Operators currently have no way to see what was sent against a booking — legacy stores outbound mail in per-day plaintext files only (`mock_up_analysis/04a-ressystem-email-inventory.md §2.4`, §8). A first-class tab on Booking Detail backed by `EmailLog` is the single biggest operator-UX gain over legacy. |
| **Editable `EmailTemplate` admin with versioning + preview-with-data + test-send.** Promotes templates from SQL-only objects to first-class operator-editable entities. Reverses the v1.1-deferral previously recorded in `product-design/04-rest-api-surface.md §2.19` and in the `Deferred` table below (`Editable-from-admin email template UI beyond basic Django admin`). | `workflows/11-integrations/email-delivery.md`, `workflows/08-quotation/transmission.md`, `10-comms.md` | `10-comms.md`, `product-design/04-rest-api-surface.md`, `product-design/05-improvements-over-original.md` | Legacy `VCEmailTemplates` is managed by raw SQL with no preview, no versioning, no test-send (`mock_up_analysis/04a-ressystem-email-inventory.md §3.1, §8`). An operator-editable admin is required to retire the SQL-only template-edit workflow. |
| **Drop hardcoded BCC + replace cleartext SMTP password storage.** Outbound mail must not BCC any internal/dev address by default; SMTP credentials on `comms.SmtpProfile` are encrypted-at-rest only (no cleartext column). | `workflows/11-integrations/email-delivery.md`, `workflows/02-administration/user-administration.md` | `10-comms.md`, `09-departures.md` | Legacy BCCs every non-quote email to `connectusinfowaydemo12@gmail.com` (`EmailService.cs:71`, `mock_up_analysis/04a-ressystem-email-inventory.md §2.3`); SMTP passwords are stored cleartext in both `VillaConfigEmail` and `UserMaster` (§2.1, §2.2). Both must not survive the rewrite. |
| **High-touch human-driven sales is the explicit product position.** Quotes are agent-crafted; clients reply informally; conversion to booking is staff-confirmed (the in-quote "Accept" link is a convenience signal, not a self-service booking creator). Automation belongs in admin / back-office workflows, not in client-side quote acceptance. | scoping-session 2026-05-26 ("we're not trying to take on Airbnb") | `product-design/00-overview.md`, `05-reservations.md`, `workflows/08-quotation/lifecycle.md` | Frames every "should X auto-happen?" call. Self-service quote-accept routes through staff confirmation rather than auto-creating the booking. |
| **Conversion is measured per `Enquiry`, not per `Quotation` (the "quote stack").** Multiple `Quotation` rows live under one `Enquiry`; any one accepted ⇒ `Enquiry.status=CONVERTED`. Reporting that reports a "conversion rate" counts enquiries. Staff UI groups every quote sent under its parent enquiry. | scoping-session 2026-05-26 ("clients book off the 5th-to-15th quote, not the first") | `05-reservations.md`, `product-design/03-workflows.md`, `workflows/08-quotation/lifecycle.md` | The data shape already supported this via `Quotation.enquiry` FK; the conversion semantics had not been stated. |
| **`BookingGuest` through-model for multi-contact bookings.** Replaces the single-`Booking.person` FK pattern (the FK survives as a denormalised LEAD pointer). Roles: `LEAD` / `CO_TRAVELLER` / `PAYER` / `CC_ONLY`. Exactly one `LEAD` per booking; at most one `PAYER`. Every traveller + payer + CC'd person is retained as an addressable `accounts.Person` row for marketing. Links to `accounts.Person` (the unified identity model — GAP-045 folded the former `reservations.Guest` into it). | scoping-session 2026-05-26 ("legacy lost ~10 years of CC'd family contacts") | `05-reservations.md`, `01-accounts.md`, `09-departures.md` | Legacy retained only the lead guest; replatform retains everyone in a queryable shape. The role enum cleanly separates a PA-payer (PAYER but not travelling) from the lead. |
| **Hold duration is per-villa default + per-hold override.** `PropertySettings.hold_duration_hours` (nullable PositiveSmallInteger) inherits from `GroupSettings`. When an agent creates a `BookingHold` without an explicit `expires_at`, the resolver picks the effective property setting. Agents may override `expires_at` directly on individual holds for trusted-owner relaxations. | scoping-session 2026-05-26 ("strict 48-hour for some, relaxed for trusted owners") | `02-properties.md`, `05-reservations.md`, `06-availability.md` | Hold tolerance varies by owner relationship; config at the villa level keeps the default honest, manual override at the moment keeps agents in control. |
| **Quotation transmission supports two send paths.** (1) Agent-from-system using `comms.SmtpProfile` (default; team uses this). (2) Copy-paste-to-Outlook via a "copy HTML to clipboard" affordance on the quote-detail view, for agents who prefer Outlook's formatting. Both paths write a `QUOTE_SENT` `EnquiryEvent`; the copy-paste path records `meta={"send_path": "manual"}` so reporting can distinguish them. | scoping-session 2026-05-26 (Nick prefers Outlook for formatting control; team uses in-app) | `workflows/08-quotation/transmission.md`, `10-comms.md`, `05-reservations.md` | Preserves operator choice without forcing in-app sender adoption. Deprecation of the manual path is revisited once in-app polish closes the formatting gap — recorded as an open follow-up below. |
| **Search hides unavailable properties by default.** Quote-builder and operator search return available properties first, with an explicit "Show unavailable" toggle to surface the legacy interleaved view. Flexible-changeover (`PropertySettings.changeover_day = ANY`) properties must be included for *every* weekday query. | scoping-session 2026-05-26 ("annoying — available at the top but unavailables interleave" + "Nick's aunt's flexible-changeover villa is missing from searches that should match it") | `06-availability.md`, `02-properties.md`, `09-departures.md`, `product-design/02-frontend-design.md` | Cleaner default; toggle keeps the legacy behaviour reachable. Two distinct legacy issues collapse to one principle: search must not hide bookable inventory. |
| **Delivery is milestone-phased; `11-milestones.md` is the authoritative phasing layer.** "v1" in the older docs means "the rebuild"; the actual first landing (M1) is narrower: property data + quoting + booking + off-WordPress checkout + occupancy + next-year rate projection. Concierge, full refund/SD lifecycle, deeper Zoho, owner portal, advanced finance permissioning re-bucket to M2+. Nothing is dropped — only scheduled. | mockup-demo 2026-05-29 ("I want to do milestones… the more we cram into scope, the more risk") | `11-milestones.md`, `INDEX.md` | A working, data-backed M1 on staging is the fastest way to de-risk the cutover from `ResSystem/`. Phasing keeps each step's scope risk bounded. |
| **Guest booking/checkout journey hosted in the SPA, not WordPress.** The booking-ref checkout page (personal info → pay) moves first-party (`portal.villacollective.com/booking?ref=…`); the WordPress round-trip and the unauthenticated `WordPressApi/*` checkout endpoint are removed. Scoped narrowly to the checkout page — the deferred post-booking **guest portal** (rows below) is **not** in scope and the two must not be conflated. | mockup-demo 2026-05-29 (Nick + Gareth agreed; "host it within the portal… moving that away would be beneficial") | `08-integrations.md`, `workflows/09-booking/booking-creation.md`, `workflows/10-payment/checkout-flow.md`, `product-design/02-frontend-design.md`, `11-milestones.md` | Removes the WordPress holding-place and its open endpoint (security win). **Honest scope note:** this *adds* a first-party checkout page + first-party Flywire return/webhook handling to M1 (currently WP-proxied) — it is not a net simplification. Flagged as an M1 risk item in `11-milestones.md`. |
| **`Enquiry.lead_status` (HOT/WARM/COLD/DEAD).** Lead temperature, distinct from the `status` workflow stage. Surfaced as an inline list dropdown that persists immediately; feeds Zoho CRM tags. Assignee (`assigned_to`), action-driven stage transitions, and loss-reason capture (`lose(reason)` + `EnquiryEvent.reason`) were **already** designed — only lead temperature was missing. | mockup-demo 2026-05-29 (res3 has hot/cold/dead + loss reasons; res2 didn't) | `05-reservations.md`, `08-integrations.md` | Closes the one genuine model gap behind the res3 lead-management surface without re-litigating the already-present assignee/stage/loss-reason design. |
| **Next-year quoting via lazy projection (carryover demoted).** When a quote lands on a year with no `RatePlan`, `PricingEngine.quote()` derives a guide rate at quote time from the most recent year that has rates (`RateProjectionService`), flags `Quote.is_projected` so the generator/email render "2028 — 2027 rates shown as a guide, inquire for accurate pricing", and **writes nothing**. Materialising editable rows is a separate, on-demand `RateCarryoverService.materialise` (admin action + `:carry-forward` endpoint), **not** a beat task — staff invoke it only when they want to hand-tune/confirm a year. Uplift defaults to 0% (verbatim). **Supersedes** the earlier carryover-by-default design: no `RateRule.is_provisional` / `carried_over_from` fields (neither was built), no portfolio-wide clone task. | mockup-demo 2026-05-29 ("default this year's rates onto next year… a huge benefit… big time saver"); refined 2026-06-03 (lazy projection, carryover demoted to on-demand promote) | `04-pricing.md`, `workflows/04-pricing/seasons.md`, `11-milestones.md` | Next-year quoting is slow because only ~10% of next-year rates exist this time of year. Deriving the guide at quote time gives the same time-saver with zero write amplification and no stale clones, and answers multiple years ahead from one path. Net-new (legacy does it by hand). The date-mapping rule is an open follow-up below — and now gates the default quoting path, not just an occasional clone. |
| **Stop Sale in the availability display vocabulary.** The operator status set is Available / On Hold / Booked / Booked-VC / **Stop Sale**, derived (via `AvailabilityService.calendar()`'s `CellStatus`) from the underlying `Booking.status` + `BookingHold.reason`. "Stop Sale" (owner using it / blocked / not for rent / booked by competitor) maps onto the existing `OWNER_BLOCK` semantics, which already carry no auto-expiry. The change is a presentation-vocabulary reconciliation in `CellStatus`, not a new hold mechanism — at most one new `reason` value if Stop Sale must be distinguished from owner-block in reporting. | mockup-demo 2026-05-29 (new status being added alongside Available/On Hold/Booked/Booked-VC) | `06-availability.md`, `workflows/06-availability/README.md` | Owner blocks were already modeled (`06-availability.md` "Owner block / maintenance"); the gap was only the operator-facing label set, which legacy expressed as status codes 10/40/50/60. |
| **Seasons stay per-property; inclusions drive how many a villa has.** *(Superseded by GAP-037 (row above, 2026-06-17): inclusions are no longer the `RatePlan.inclusion` free-text that drove "how many seasons"; they are first-class `properties.PropertyService` rows with their own date bands and tab. The **per-property** shape and the "no portfolio-wide standard-season restructure" call still hold — only the inclusions-drive-season-count framing is overturned.)* A villa has multiple seasons (RatePlans/cards) only when service **inclusions** differ across the year — inclusions are the operator's mental driver of "how many seasons". `RatePlan.inclusion` stays the descriptive field; no first-class `Inclusion` entity and **no** portfolio-wide standard-season restructure (the per-property shape is data-backed: 96% of legacy seasons had a single date range). VC-standard reporting labels (peak/shoulder/low) are *wanted* but their placement is an open follow-up below — no field added speculatively. | mockup-demo 2026-05-29 (explored standardising seasons + Season→Inclusions→Rates layering; agreed light touch) | `04-pricing.md` | Honours the operator mental model and the production-data finding without re-architecting pricing for a reporting convenience that has no named consumer yet. |
| **No single property-wide primary contact; primacy resolves per role (GAP-027).** `one_primary_per_role` (partial unique on `(property, role)` where `is_primary=True, end_date IS NULL`) already lets a primary OWNER and a primary MANAGER coexist. Consumers pick "the" primary by purpose: commercial/sales → primary OWNER; operations/concierge → primary MANAGER (fallback HOUSEKEEPER); finance → primary OWNER unless an OWNERS_REP is primary. No property-level `is_primary` is added; any UI showing "the primary contact" must label which role it resolves. Documentation-only — the constraint already exists in `properties/models/contacts.py`. | `todo/gap-027`; 2026-06-11 new-villa setup transcript | `02-properties.md` | The loader's "primary" is per-purpose, not per-villa (the owner is her commercial primary; the villa manager is the concierge team's operational primary). The schema already supported it via `one_primary_per_role`; only the convention was unwritten — and an inline-created contact now auto-selects back into the picker (the other half of GAP-027). |
| **Money inputs adorn with the resolved currency; multi-currency per property is allowed with a soft warning (GAP-026).** `PropertySettings` exposes a read-only `currency_code` — the group-resolved effective currency as a string code (raw `currency` FK's inheritance already applied; `None` when neither property nor group sets one). Rate/season price inputs adorn from the rate plan's own `currency_code`; fixed finance amounts adorn from the property's effective `currency_code` (a `percent` amount shows `%`; an inherited-type amount stays unadorned since its basis lives on the unseen group). A season whose currency diverges from the property's shows a **non-blocking** warning, never a block — mixed currencies are intentional. A `fixed` amount with no resolved currency shows a "set a currency" prompt, not a blank prefix. | `todo/gap-026`; 2026-06-11 new-villa setup transcript | `02-properties.md`, `04-pricing.md` | The currency is a display/label hint, not an enforced single-currency rule: a villa can legitimately quote in EUR while its owner settles commission in GBP. Surfacing the unit beside every amount removes the ambiguity without constraining the data; resolving the *effective* code server-side keeps the client from re-walking the group-inheritance chain. |
| **The deduplicated traveller directory; integrity is honest, not synthetic.** *(Superseded in part by GAP-045 (row above, 2026-06-18): the traveller model is now `accounts.Person`, not a distinct `reservations.Guest`, and email/phone moved to the `PersonEmail`/`PersonPhone` child tables.)* Email is **optional** (no fabricated address); phone is stored **normalized E.164** (`phonenumbers`). Dedup is **advisory** (resolve-or-create suggestion + operator-confirmed `Person.merge()`), never a hard unique. The create-time synthetic `enquiry-{id}@noemail.local` is removed (optional email); `Person.anonymize()` replaces each `PersonEmail.email` with a `redacted-{id}@anonymized.local` sentinel (the child-table column is `NOT NULL`). `Enquiry` gains a denormalised `contact_method`. | 2026-06-08 data-model deep-dive; legacy `VillaClientDetail`/`sp_clientDetails` (per-enquiry, `""`-filled NOT NULL, destructive email-upsert) | `people-model-cleanup.md`, `05-reservations.md` | The spec already said the traveller record is "reused", but the implementation created a fresh, never-deduped row per quote and fabricated emails to satisfy a fake `NOT NULL`. Enforce the real invariant (reachable by ≥1 channel) and make dedup a first-class, advisory operation. |
| **`Quotation.enquiry` is `NOT NULL` (`PROTECT`); agent-direct quotes auto-create a minimal enquiry.** Reverses the prior nullable-bridge (*"quotations can be created agent-direct without an enquiry"*). The quote-creation service back-creates an enquiry when none is supplied (legacy `sp_quotationMaster` parity), tagged via the existing `site_source` enum so per-`Enquiry` conversion reporting can segment them. Migration backfills existing `enquiry IS NULL` rows first. | 2026-06-08 data-model deep-dive; legacy `VillaQuotationMaster.EnquireId NOT NULL` + `sp_quotationMaster` auto-enquiry | `people-model-cleanup.md`, `05-reservations.md` | Every quote belongs to an enquiry (the unit of conversion measurement). Auto-creation satisfies the invariant without a sentinel, a nullable orphan FK, or a forced separate capture step. |
| **`Payment` is the generic money ledger (Q-016 Lane A); `SecurityDeposit` is lifecycle detail, not a parallel ledger.** `SECURITY_DEPOSIT` stays in `PaymentPurpose`: every money flow for a booking — deposit, balance, security deposit, concierge, refund — is a `Payment` row, so "all money for booking X" stays one query with one set of webhook handlers and state machines. The `SecurityDeposit` model remains as the post-capture lifecycle record (hold/return/deduction, damage-claim linkage) layered over its `Payment` row, never an alternative source of money truth. Cardinality is enforced on the ledger via **three per-purpose `UniqueConstraint`s** (`unique_active_deposit` / `unique_active_balance` / `unique_active_security_deposit`, each scoped to `ACTIVE_PAYMENT_STATUSES`) rather than one blanket `purpose__in` constraint — see the BUG-006 resolution (SD hold superseded by capture) for the cardinality table. Closes Q-016; BUG-006 and FG-004 were scoped under this lane, and BUG-008 stays inside `SecurityDeposit` as the lifecycle system of record. | Q-016 decision 2026-06-12 (lane already taken implicitly in code); `payments/models/payment.py` constraint set; `todo/bug-006-payment-active-purpose-uniqueness.md` | `07-payments.md`, `todo/q-016-payment-ledger-vs-dedicated-models.md` | A hybrid where callers must know which of two models is authoritative has the cost of both and the clarity of neither. One ledger keeps reporting, webhooks, and uniqueness rules in a single place; the deposit-specific lifecycle (returns, deductions, claims) genuinely differs and earns its own model — as a view over the ledger, not a second ledger. |

| **Occupancy-band fan-out is the quote-builder default (Loom 2026-06-17) — reverses "salesperson curates".** For an occupancy-priced property the builder presents **all** covering bands as separate lines **checked by default**; the agent deselects. Supersedes the `04-pricing.md` note that multi-bracket display is a selective agent action with no automatic fan-out. **Engine contract unchanged** — `PricingEngine.quote()` still resolves one band per call; the builder drives the fan-out. **Extended by GAP-044b (shipped 2026-07-01):** the banded card also gets the **week picker** GAP-044 had suppressed — bands now price the *selected* week (default block or a lazily-repriced alternate), not the default block only; each checked band saves at the chosen week's dates. Bands stay alternatives; the engine stays single-band. | owner Loom 2026-06-17 ("better to quote all of the occupancy-based pricing… full visibility if the group size changes") | `04-pricing.md`, `todo/done/gap-044-occupancy-band-fanout-builder.md` (shipped 2026-07-01), `todo/done/gap-055-occupancy-band-week-picker.md` (GAP-044b, 2026-07-01) | Owner wants the client to see price-by-group-size by default; the prior "curate" default is retained in-doc as superseded, not deleted. The engine stays single-band so pricing truth is unaffected. |
| **Multi-week range quoting is wanted (Loom 2026-06-17) — tension with the `flexibility_days` rework, not yet a hard reversal.** The builder should quote a **date range** with **per-week selection** (tick each week). Whether this *replaces* or *coexists* with the ±n-day stepper is left open for the build; the no-destructive-shift `flexibility_days` model still holds. The mockup's Flex? presets (`Specific dates`/`+/- 3 days`/`+/- 7 days`/`Flexible`) reinstate the full legacy `EnquireDateTypeString` set, wider than today's 0–3 cap. | owner Loom 2026-06-17 ("fixed price, fixed dates… not correct… we do a date range… click all the weeks") | `05-reservations.md`, `04-pricing.md`, `todo/done/gap-043-quote-builder-multi-week-range.md`, `todo/gap-039-enquiry-dashboard-enrichment.md` | Graduates the "flexibility wider than ±a few days" rough edge into a requirement; recorded as a tension so the prior rework's rationale is preserved while the replace-vs-coexist call is made with UX. **Resolved by the GAP-043 row below (2026-07-02): replace.** |
| **Quote-builder week selection: full-width while building + horizontal week strip + held weeks non-selectable** (2026-07-01). While the builder is open the enquiry-page right rail is hidden (`TwoColumn hideRail` — `display:none` but still **mounted**, so a half-typed note survives) so results get the full page width; the builder already renders its own enquiry summary, so the rail was duplicating it while stealing ~340px. The multi-block week picker (`StayOptionPicker`) is a compact **horizontal, scrollable strip** of changeover-week cells (compact `formatWeekRangeCompact` range labels) — replacing the old wrapping verbose-pill row — kept as a roving-tabindex + Arrow-key ARIA radiogroup. **Held (booked) weeks are non-selectable**: the cell is `disabled`, Arrow-nav skips it, and the caller (`QuoteResultLine`) preselects the first bookable block; an all-held result shows its requested week disabled with Add correctly gated (`heldSelected`). **Reverses** the interim "held stays selectable so the operator can peek at its price" contract — a week you can pick that only dead-ends into a disabled *Add to quote* was more confusing than the niche price-peek was worth. | `/ship` quote-week-strip plan (2026-07-01) + owner visual review; `frontend/.../quotations/components/StayOptionPicker.tsx`, `layout/TwoColumn.tsx`, `enquiries/EnquiryDetailLayout.tsx`, `quotations/QuoteResultLine.tsx` | `product-design/03-workflows.md` §2 (builder screen; still describes the aspirational two-pane/cart shape), `todo/done/gap-043-quote-builder-multi-week-range.md` (the multi-week rework, shipped 2026-07-02, inherited non-selectable-held), `todo/done/gap-055-occupancy-band-week-picker.md` | Presentation + selectability decision on top of GAP-044/044b's pricing axes; single-select today (one changeover block per result), so held-non-selectable is the UX floor GAP-043's multi-week model must preserve. |
| **Quote-builder search is an arrival window + weeks; week strip is multi-select (GAP-043, shipped 2026-07-02) — replaces the fixed-dates + ±flex stepper.** The criteria form is the mockup's Rate Lookup shape: **Arrive from + Arrive to + Number of weeks + Search Specific Date** (legacy `IsSpecificDate`); the ±`flex_days` stepper is gone. Replace-vs-coexist (left open in `05-reservations.md`) resolved as **replace**. Client-side translation maps the window onto the unchanged `search-options` contract (`flex = ceil(W/2)`, midpoint preferred arrival, never earlier than Arrive-from; 42-day cap = 2 × `SEARCH_FLEX_MAX`); the enquiry seeds the window symmetrically (`date_from ± flexibility_days`). **Reverses the 2026-07-01 single-select picker**: `StayOptionPicker` is now a multi-select checkbox group — every ticked week stages its own quotation line (`line_id = property:date_from`), weeks × checked bands fan out at save — while **preserving that decision's held-non-selectable floor** (held cells disabled + arrow-skip) and the horizontal-strip presentation. Intake `flexibility_days` widening + "Flexible" mode stay deferred to GAP-050 item 7. | owner Loom 2026-06-17 + mockup Rate Lookup; `/ship` gap-043 plan (2026-07-01) | `05-reservations.md` §"Date flexibility on intake" (resolution note), `todo/done/gap-043-quote-builder-multi-week-range.md`, `frontend/.../quotations/{QuoteCriteriaForm,StayOptionPicker,QuoteResultLine,QuoteBuilder}.tsx`, `searchCriteria.ts` | The owner's core ask ("click all the weeks we want to quote") requires multi-select; a fixed-date + stepper form can't express an arrival window. Backend contract untouched keeps pricing truth centralised. |
| **Customer 360 profile + tags + standing linked contacts (Loom 2026-06-17) — new CRM area.** A full client profile for the sales team: identity + hideable address + notes + **operator tags** (VIP/Repeat/Trade/PA/Nick's friend/Nick's network/Disability/Approach-with-care/Past-issues/Specific-preferences/Time-waster), **standing person links** (spouse/child/PA, distinct from per-booking `BookingGuest` roles), and aggregated history (enquiries/quotes/previous bookings). "Calls" in the history has **no backing model** — out of scope or a new activity-log decision. | owner Loom 2026-06-17 ("a very full customer profile for the sales team") | `01-accounts.md`, `05-reservations.md`, `product-design/03-workflows.md`, `todo/gap-040-customer-tags-taxonomy.md`, `todo/gap-041-standing-linked-contacts.md`, `todo/gap-042-customer-360-profile-view.md` | None of tags / standing links / consolidated profile existed in the model; "Repeat" should be derived, "PA" overlaps the link role — resolve before seeding the taxonomy. |
| **Enquiry list enrichment + stage-taxonomy reconciliation (Loom 2026-06-17).** The list adopts the Ben/owner mockup's columns/filters (region, salesperson inline-assign, holiday-date range, Flex?, stage, lead-status+reason). The mockup pipeline (New Enquiry → Progressing → Quote Sent → Follow-up; Dead+reason) must be reconciled with `Enquiry.status` (NEW/CONTACTED/QUOTED/LOST/CONVERTED). The **quotes-to-convert** metric is surfaced on top of the already-recorded per-`Enquiry` conversion (above) and `lead_status` (above). | owner Loom 2026-06-17 + mockup https://vc-new-res-system.netlify.app/ | `product-design/02-frontend-design.md`, `product-design/03-workflows.md`, `05-reservations.md`, `todo/gap-038-enquiry-quote-stacking-conversion-metric.md`, `todo/gap-039-enquiry-dashboard-enrichment.md` | Refines, not replaces, the per-enquiry conversion + lead-temperature decisions; the new work is the richer grid, the stage vocab, and the conversion *metric/reporting*. |
| **Per-property feature ordering via `PropertyFeature` through model (GAP-022, 2026-06-18) — reverses "plain M2M wins".** Property ↔ Feature is an explicit `PropertyFeature(property, feature, sort_order)` through model, not a plain auto-through M2M. The legacy `VillaFeaturesMappings` carried `MappingOrder` (operator-chosen per-villa display order); the plain M2M dropped it, regressing the public-site/editor render. The write serializer maps the ordered `feature_ids` list position → `sort_order` (diff-write, so a pure reorder logs only moved rows); the loader carries `MIN(MappingOrder)` across (dedup is load-bearing — the legacy table has no unique on the pair and the prod snapshot has duplicate pairs); FeaturesTab is a flat drag-drop list; `PropertyFeature` is audit-tracked (FG-017). | GAP-022; 2026-06-11 new-villa setup transcript review | `02-properties.md`, `properties/models/features.py`, `properties/serializers/property.py`, `data_migration/loaders/property_children.py`, `frontend/.../tabs/FeaturesTab.tsx`, `todo/done/gap-022-per-property-feature-ordering.md` | Per-villa feature order is real operator judgement (legacy `MappingOrder`), not derivable — so it needs first-class storage. The through model is the off-the-shelf Django mechanism; the diff-write keeps audit noise down on reorders. |
| **Net↔gross rate entry derives on display, never persists; `RatePlan.price_basis` is the sole pricing authority (GAP-035, 2026-06-22).** Staff type one figure and pick the basis; the rate-band form shows the **derived counterpart** (owner net for a GROSS plan, guest price for a NET plan) live, computed with the **same** mode-aware commission **+ tax** math as the engine's `04-pricing.md` steps 8-9 (legacy `RatesModel.Calculate()`) — a percentage commission grosses up by `÷(1−pct)` (×1.25 for 20%), a fixed commission is flat both ways, tax is skipped when exempt, rounding is `ROUND_HALF_EVEN`. (BUG-009 landed 2026-07-02: the engine implements the same maths in the same quantization order — raw commission feeds the tax base, rounding at the end — so the hint matches the engine's quote to the cent.) **Only the typed figure + `price_basis` are stored** (derive-on-display) so the derivation never double-counts against the BUG-009 engine carve-out. Commission/tax come from `PropertyFinance.effective_commission()`/`effective_tax_policy()` resolved property→group, surfaced read-only on the **settings** endpoint (`commission`, `tax`, `prices_entered_as_effective`) beside `currency_code`. **Single-source-of-truth fix:** `PropertySettings`/`GroupSettings.prices_entered_as` is **demoted to a default** that pre-fills a *new* season's `price_basis` — basis is a per-plan property (a villa runs a GROSS public plan and a NET agent plan at once), so the engine reads `RatePlan.price_basis` only; `prices_entered_as` is no longer a second pricing field, just the entry-default seed. | owner Loom 2026-06-17 (pricing walkthrough 2:40 — "put in 20% commission and it automatically calculates the gross"); `todo/gap-035-net-gross-commission-derivation.md`; BUG-009 single-source-of-truth note | `04-pricing.md`, `properties/serializers/settings.py`, `properties/views/settings.py`, `frontend/src/lib/pricing/netGross.ts`, `frontend/.../components/RateRuleFormDialog.tsx`, `frontend/.../components/SeasonFormDialog.tsx`, `todo/done/gap-035-net-gross-commission-derivation.md` | Owners quote in either basis; deriving the counterpart at entry removes the manual conversion without persisting a second figure that the BUG-009 engine carve-out would re-derive — entry and quote-time must agree on one stored number. Demoting `prices_entered_as` ends the "two basis fields" ambiguity the BUG-009 spec flagged while keeping a useful per-property default. |

## Drops vs the prior decisions pass

The prior plan (`deeply-analyze-the-data-stateful-gizmo.md`) proposed these; the revised pass intentionally **does not** apply them.

| Proposed | Why dropped |
|---|---|
| `Enquiry.flexibility` 5-value enum | Existing `Enquiry.is_flexible` boolean is sufficient for v1; the four legacy ranges (`±3d`, `±7d`, `month`, `any`) weren't load-bearing in the workflow. **2026-06-08 demo caveat:** the `month` / `any` cases are *real* ("any week in June", "one of these three weeks") and the owner flags current-system pain here. Still dropped for v1, but revisit if multi-week flexible search becomes a quoting bottleneck — see `05-reservations.md` date-spread heuristic ("Flexibility wider than ± a few days"). |
| `Booking.party_size_confirmed_at` column | `PaymentScheduler` can check `Booking.status > CONFIRMED` — no new column needed. |
| `Booking.group_reference` UUID | Group bookings (multi-villa under one Booking) are explicitly out of scope for v1. |
| `Payment.received_amount` / `Payment.received_currency` | Multi-currency settlement capture isn't in v1 workflows; defer. |
| `Payment.reminder_sent_at` | Reminder idempotency rides on an `EmailLog` correlation query — no new column. |
| Named `PartyOutOfRange` exception added separately | Engine already raises typed exceptions; this is one more in the family (documented in `04-pricing.md`), not a new design call. |
| `django-axes` rate-limiting in this pass | Real concern, but a cross-cutting security ticket, not part of this design pass. |
| `Property.requires_full_day_changeover` flag | Half-day changeover is out of scope for v1; full-day is the implicit default — no flag needed. |
| Per-module Zoho **Serializer classes** named explicitly in the design | Implementation detail. The generic `SyncClient` hierarchy is enough at the spec level. |
| `SyncRecord.meta` JSONField | `SyncIssue.local_state` / `remote_state` already capture what's needed for drift inspection. |

## Deferred (recognised, out of v1)

These are real legacy or workflow concepts that the v1 design intentionally does not cover. Each becomes a v2 (or later) discussion when the business need re-emerges.

| Item | Reason |
|---|---|
| **Client / guest portal** (post-booking guest self-service: itinerary view, in-app messaging, concierge ticketing, arrival info reveal, co-traveller invites, guest-initiated payment). Mockup: `mock_up_analysis/02-client-portal.md`. | The legacy `ResSystem/` has no analog — the only guest touchpoint is the WordPress checkout form (`workflows/10-payment/checkout-flow.md`). The portal is ~80% greenfield: new auth model, new `Thread`/`Message`/`ServiceRequest` entities, new guest-driven concierge approval flow. Too speculative for v1; revisit once the operator-facing rebuild is live and we have real usage signal. |
| **Owner portal beyond read-only view** (multi-user owner-org accounts with internal RBAC, owner-initiated content edits with a VC approval queue, owner-driven property onboarding, in-portal messaging). Mockup: `mock_up_analysis/05-owner-portal.md`. | The legacy `ResSystem/` has no owner-facing tooling — owners interact via staff/email. The mockup goes meaningfully beyond `product-design/02-frontend-design.md §7.3` and `03-workflows.md` flow 14 (which only commit to read-only bookings/statements + simple block requests). The four expansion axes (multi-user org, change-request approval queue, owner-driven onboarding, messaging) each require new entities and have no legacy basis. Keep the v1 owner surface to the spec'd read-only minimum; defer the rest. **2026-06-08 demo addition:** the owner named a specific competitor pattern worth folding into this work — owners are **emailed a one-click link to update their own availability** on a regular cadence, attacking the same "time spent chasing owners for availability updates is time when clients are shopping around losing interest" pain that iCal ingest (`todo/done/gap-011-ical-feed-ingest.md`) attacks from the other side. The two are complementary (iCal pulls feeds for the ~30% who publish them; the email-link refresh covers the rest). Both feed the deferred owner-portal / availability-self-serve work; neither is M1. (iCal ingest has since been built — ✅ resolved 2026-06-22; the email-link self-serve refresh remains deferred owner-portal work.) |
| Group bookings (multi-villa under one `Booking`, shared payment) | Niche in legacy; one `Booking` = one `Property` for v1. |
| Half-day changeover boundaries | Range-model `[date_from, date_to)` makes half-day modelling expensive; owner blocks the day instead. |
| Per-villa email-template branding overrides | Legacy didn't have it; resist gold-plating. |
| Multi-language email templates | No v1 use case. Locale field on `EmailTemplate` is the obvious extension. |
| ~~Editable-from-admin email template UI beyond basic Django admin~~ | **Reversed** — now live: see the "Editable `EmailTemplate` admin with versioning + preview-with-data + test-send" row in `Live decisions` above. |
| WordPress → Canary bidirectional sync; multi-site fan-out | Single public site for v1; `SyncRecord(provider=WORDPRESS_SITE)` is sufficient. |
| Channel-manager integrations (Booking.com, Vrbo) | Out of v1; would land as new `integrations.SyncClient` subclasses. |
| Multi-currency payment-settlement capture | v2 once a gateway with multi-currency settlement is selected. |
| Pre-auth live wiring (provider call layer) | The state machine and `SecurityDeposit` model exist; gateway integration is v2. |
| Concierge personalisation / per-recipient quote-line metadata | Legacy doesn't have it. |
| `GuestPreference` model (legacy `VillaClientPrefMaster` / `ClientPreferenceDetail`) | Underused in legacy; can re-add later as a structured model. |
| Per-villa rental alternatives / "rent together" bundles (legacy `VillaRentalAlternative`) | Future scope. |
| `BookingPaymentMethod` (saving multiple cards-on-file per booking) | One card-on-file token on the booking is enough for v1. |
| **iCal feed ingest from owners** (per-villa public iCal URL pulled into availability) | **✅ BUILT (2026-06-22) — no longer deferred.** Originally a deferred v2 force-multiplier (scoping-session 2026-05-26; re-emphasised 2026-06-08 demo — "a game changer", "a big problem in the business"); the engine landed and is beat-scheduled. Shipped: `SyncProvider.ICAL` + `PropertyCalendarFeed` model, the poller (generic parser + per-source profiles, cross-feed coalescing, stale-event release, composite-key upsert), the ops **conflict alert**, and the in-app `OwnerBlockUpdate` **awareness feed**; the feed `url` is kept secret end-to-end. **One deferred sub-item:** the batched per-poll awareness *digest email* (the in-app feed + conflict alert are deemed sufficient). **Volume discrepancy still open:** "~30 villas" (2026-05-26) vs "**about 30%** of villas" (2026-06-08 demo) — reconcile against the real catalogue when tuning poll cadence. Resolution, verified assumptions, and remaining deferrals: `todo/done/gap-011-ical-feed-ingest.md`. |
| **Per-person base pricing (Kenya safari model)** — base nightly rate scaled by occupant count rather than per stay. | Scoping-session 2026-05-26 raised this for the Kenyan property mix; not urgent in MVP. Revisit if Kenya becomes a meaningful share of bookings. The existing `Extra.calc = FIXED_PER_PERSON` / `FIXED_PER_PERSON_PER_NIGHT` covers per-person *fees* but not per-person *base rate*. |
| **Price-basis-aware engine maths (GROSS carve-out / NET gross-up) — BUG-009.** **✅ BUILT (2026-07-02) — no longer deferred.** Originally deferred to the finance rewrite; landed independently (user call, 2026-07-02) once it was clear the maths only need pct / fixed / exempt, which already flow through the `_call_finance_resolver` shim. Shipped: `PricingEngine._derive_commission_and_tax` branches on `RatePlan.price_basis` per `04-pricing.md` steps 8-9 (raw-commission-feeds-tax-base quantization order, ≥100%/zero-base sanitisation guards as a documented legacy divergence), the breakdown snapshots `price_basis` + `net_to_owner`, and the FE probe workaround is unwound (the probe trusts the engine `total` and shows owner economics). The shim (and its dict/attr tolerance) **stays** until the real finance rewrite. | Resolution: `todo/done/bug-009-price-basis-ignored-by-engine.md`. The single-source-of-truth with `prices_entered_as` was **resolved** earlier (GAP-035, 2026-06-22 — `RatePlan.price_basis` is canonical; `prices_entered_as` demoted to the entry default): `todo/done/gap-035-net-gross-commission-derivation.md`. |
| **Room/image bedroom-tagging** — tagging property images with a bedroom label (Bedroom 1/2/…) so the public site can caption enlarged images, and notes can reference a specific room. | Surfaced 2026-05-29 as a real pain ("you don't know which room you're referring to"), but blocked on a large manual data effort (someone must revisit each villa). Cheap to model later (a nullable `room`/label FK on the image), but not worth building UI for until the data exists. Not M1. |

## Open follow-ups

These need an answer before implementation; tracked here so they don't get lost in workflows / docs.

- **GAP-064 room-attribute vocabulary shipped on ticket defaults — A1 owner
  confirmation pending (2026-07-05).** The A1 steer (ensuite shower/bath/both
  distinction; the 9-row starter amenity catalog; which amenities imply a
  property feature) was never answered, so GAP-064 shipped the ticket
  defaults: `ensuite_type` trio with `""`=unknown, starter rows aircon /
  ceiling_fan / sea_view / balcony / terrace / wheelchair / in_room_safe /
  hairdryer / mini_fridge, `sea_view`→`sea-view` implication (candidate-slug,
  set-if-NULL), `wheelchair` implication left NULL (no accessibility Feature
  exists yet). Everything vocabulary-shaped is admin-editable data
  (`RoomAttribute` rows + the `implies_property_feature` FK in Django admin),
  so the owner can revise without a developer; if she doesn't track the
  shower/bath distinction the `ensuite_type` column simply stays `""`. Confirm
  with Bryony alongside the GAP-065 A2 questions.
- **Owner-approval SLA**: `workflows/09-booking/booking-creation.md` describes owner approval but doesn't specify a default reminder cadence or auto-decline window. The `escalate_pending_owner_approvals` Celery task needs a threshold (currently `[CONFIGURABLE]`).
- **Cancellation-fee pre-window behaviour**: current design uses a single `cancellation_window_days`. A common pattern is sliding bands (e.g. 100% > 60 days, 50% 30-60d, 0% < 30d). Out of v1; add a `cancellation_bands` JSONField on `PropertyFinance` if it lands.
- **`DamageClaim` model** *(was Open ticket; resolved 2026-06-23, BUG-008)*: shipped in `reservations/` (`reservations/models/damage_claim.py`) — booking + currency FKs, amount, `DamageClaimStatus`, a DC reference sequence, and the `itemized_lines`/`photos`/`accepted_by_guest_at` scaffolds for the fuller spec. `SecurityDeposit.damage_claim` is now a real `FK(SET_NULL)`, resolved + booking-validated in `SecurityDepositService.claim()`. The damages *workflow* (operator report sub-form, photo upload, threshold permissions, the damages email, the enforced approval state machine) stays deferred to workflow 8/17.
- **Lead-management primacy: Res vs Zoho.** The sales team may move inquiry management into Zoho CRM. Two shapes are on the table — (a) Res-primary, Zoho mirrors via the existing `ZohoSyncClient`; (b) Zoho-primary for leads, Res consumes via inbound pull. Decision deferred pending sales-team and product-team interviews after the 2026-05-26 scoping session. The MVP will be Res-primary (current shape); flipping primacy is a v2 conversation. Surfaced in `08-integrations.md`.
- **`Booking.person` FK survives under `BookingGuest`** *(was `[OPEN]`; resolved at implementation; GAP-045 renamed the FK `guest`→`person`)*. `Booking.person` is kept as a denormalised pointer to the LEAD guest, kept in sync by `_booking_guest_post_save` on `BookingGuest`. `_booking_guest_pre_delete` raises `LeadGuestProtectedError` if a LEAD row is deleted while its booking still exists (cascade-safe via the `origin` kwarg). `BookingService.create_from_quotation_line` creates the `BookingGuest(role=LEAD)` row inside the same `transaction.atomic` as the `Booking`, so the LEAD invariant is exercised from production day one — including the legacy `data_migration.loaders.bookings.BookingLoader` import path.
- **Quotation send-path consolidation.** The "copy HTML to clipboard" alternate send path (decision row above) is preserved for v1. Whether to deprecate it in v2 once the in-app sender's formatting reaches parity with Outlook is open — track via the `EnquiryEvent.meta.send_path` distribution. Surfaced in `workflows/08-quotation/transmission.md`.
- **Carryover date-mapping rule** *(transcript-ambiguous; owner to resolve — now higher-urgency)*. When a year is projected/carried forward, does a rule keep the **same calendar dates** (`keep_calendar_date`) or **shift to align the changeover weekday** (`shift_to_changeover_weekday`, `ChangeOverRule`)? The 2026-05-29 transcript ("the seventh this year will be the seventh next year whichever way around") does not settle it. The function is injected (defaulting to weekday-alignment) and shared by `RateProjectionService.project` and `RateCarryoverService.materialise`, so nothing is hard-coded — but because projection is the **default next-year quoting path**, the default now affects every projected quote, not just an occasional clone. Confirm against Bryony's listing Loom before treating it as settled. Surfaced in `04-pricing.md` / `workflows/04-pricing/seasons.md`.
- **VC-standard season reporting-label altitude** *(owner to resolve)*. Peak/shoulder/low reporting labels are wanted (2026-05-29), but the operator "season" is a `RatePlan` while high/mid/low is a `RateCard` — a portfolio reporting band most likely belongs on `RatePlan` (or a small lookup), not `RateCard`. No field is added until a named reporting consumer exists and the altitude is chosen. Surfaced in `04-pricing.md`.
- **Booking-flow detail pending walkthrough.** The 2026-05-29 call did not cover the booking journey in depth; a dedicated walkthrough call + Ben's res3 Loom are pending. Re-validate `workflows/09-booking/*` and the SPA checkout against them. The 2026-06-08 demo surfaced three booking-stage pains to fold into that walkthrough: (a) **security deposits are "a nightmare"** — the owner deferred the detail to the booking walkthrough itself; re-validate `07-payments.md` `SecurityDeposit` against the real failure modes once captured. (b) The **create-payer-profile UX is confusing** on the checkout/booking page and needs redesign — the `PAYER` `BookingGuest` role exists and is correct, but the affordance "would confuse people"; this is a UX problem, not a model gap. (c) The **owner booking-confirmation leaked gross instead of net rates** in legacy — verify the owner-facing view reads `net_to_owner` (now addressed in `04-pricing.md` `RatePlan.price_basis`).
- **Property-field stripping pending Ben's Loom.** Ben's res3 mockup removed surplus property fields. Reconcile the trimmed set against `02-properties.md` once the Loom lands; treat property structure as canonical until then (carried over from prior vendor "16i").
- **Owner-portal M2 discovery: The Collectionist.** The owner referenced The Collectionist's owner-availability UX (owner app; scroll availability + open text field) as the market-leader pattern to study before building owner self-serve availability + onboarding. Feeds the deferred owner-portal work (Deferred table) — not M1.

## How to use this file

When you are deciding whether a behaviour belongs in the design:

1. Grep this file for the area first (e.g. "concierge", "refund", "SMTP").
2. If the row exists and is **live** — implement against it.
3. If the row exists and is **deferred** — push back and surface this file as the basis for not doing it.
4. If the row exists and is **dropped** — surface this file as the basis for not re-raising it.
5. If no row exists — propose a new decision via a PR that adds one here. Status starts as **live** only when the docs are updated to match.
