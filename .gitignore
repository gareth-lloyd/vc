ResSystem/
ResSystem-prod/
.DS_Store

# Python / Django
__pycache__/
*.py[cod]
*.egg-info/
.venv/
db.sqlite3
db.sqlite3-journal
staticfiles/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Node / Vite
node_modules/
dist/
.vite/
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# Editor / OS
.idea/
.vscode/
*.swp

# Dev-server logs from `make dev-backend` / `dev-frontend` (tee'd to disk so
# Claude / scripts can tail them).
logs/

# Env files (never commit secrets)
.env
.env.local
.env.*.local

# pre-commit
.pre-commit-cache/

# Claude Code harness scratch (agent worktrees)
.claude/worktrees/

# Generated media from local dev seeding (MEDIA_ROOT = django_res/media).
django_res/media/
