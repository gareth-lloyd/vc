name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  backend:
    name: backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: django_res
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: villacollective
          POSTGRES_PASSWORD: villacollective
          POSTGRES_DB: villacollective_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U villacollective"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      DATABASE_URL: postgres://villacollective:villacollective@localhost:5432/villacollective_test
      DJANGO_SETTINGS_MODULE: villacollective.settings.test
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: django_res/uv.lock

      - run: uv sync --frozen

      # Ephemeral CI DB — disable durability so fsync-bound COMMITs in the
      # (parallel) test suite stop waiting on the disk. All three params are
      # SIGHUP-context, so a reload applies them with no restart. Zero risk:
      # the service container is thrown away when the job ends.
      - name: Disable Postgres durability
        env:
          PGPASSWORD: villacollective
        run: |
          psql -h localhost -U villacollective -d villacollective_test \
            -c "ALTER SYSTEM SET fsync = off;" \
            -c "ALTER SYSTEM SET synchronous_commit = off;" \
            -c "ALTER SYSTEM SET full_page_writes = off;" \
            -c "SELECT pg_reload_conf();"

      - run: uv run ruff check
      - run: uv run ruff format --check
      - run: uv run mypy .
      - run: uv run lint-imports
      - run: uv run pytest

  frontend:
    name: frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test -- --run
      - run: npm run build
