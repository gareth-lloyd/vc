# Critique — 2026-06-19

Five parallel review agents re-assessed every **open** ticket in this folder
(51 at start of review) against the current `django_res/` + `frontend/`
codebase, the way `CRITIQUE-2026-05-27.md` did three weeks and ~500 commits
ago. This file records the synthesis. The earlier critique is now fully worked
through (its tickets resolved; banner added) — this is its successor.

Method: each agent owned a cluster, read each ticket in full, then **verified
the premise against actual code** (not the ticket's own claims) and classified
it VALID / ALREADY-RESOLVED / PARTIAL / STALE-PREMISE / MISSCOPED / DUPLICATE /
BLOCKED. Headline: the backlog is in good health — most open tickets are
genuinely valid. The rot is concentrated in (a) one mis-filed resolved ticket,
(b) a handful of stale premises where recent commits changed the ground, and
(c) cross-ticket sequencing that isn't written down.

## Mis-filed — move to `done/` (1)

- **FG-009** — **ALREADY-RESOLVED.** The proposed decoupling shipped: a
  dedicated `CsrfView` at `auth/csrf` (`accounts/views/auth.py:41,53`), FE
  `primeCsrfCookie()` (`frontend/src/app/boot.tsx:6,25`), and regression tests
  (`app/__tests__/bootCsrf.test.tsx`). INDEX's "⬜ low priority" is stale.

(GAP-036 was also flagged by review as resolved — it was merged to `done/`
mid-review via `be5ecde`, so it is already filed; no action.)

## Backlog claim contradicts the code (2)

- **BUG-009** — INDEX:28 says "spec done; code deferred." The **code** defer is
  correct (engine still does `base × pct/100` at `engine.py:535-545`, never
  reads `price_basis`), but the **spec** slice is *not* written: `04-pricing.md`
  steps 8–10 carry no GROSS carve-out / NET gross-up wording and the engine TODO
  has no BUG-009 ref. Either write the spec slice or correct INDEX — don't claim
  "spec done."
- **GAP-012** — code is fully merged (PR-A storages config + `import_legacy_images`);
  only ops-runbook steps remain. INDEX's 🟨 "code complete" is fair; treat as an
  ops task, not a code gap.

## Stale premise / mis-scoped — rewrite before implementing (5)

- **FG-005** — premise rests on a **dead table**. `IdempotencyRecord` has zero
  runtime writers (`grep IdempotencyRecord.objects` → only the class def); live
  dedupe is `core/idempotency.py` meta-key stamping on per-model `meta` JSON.
  The "user-required FK blocks system actors" framing is moot. Reframe to the
  real question (INDEX:34 already has it): **delete the dead table vs revive it
  as the real backstop.** The FK fix only matters under "revive."
- **GAP-020** — banks on **folding SMELL-014** (§1), but SMELL-014 closed
  2026-06-18 by keeping the string-prefix `.real()` (`quotation.py:23-39`), *not*
  by the `kind` enum GAP-020 assumes. The `kind` refactor is now net-new scope,
  not a free rider — rewrite §1 + the dependencies, or drop it for the simpler
  prefix path.
- **GAP-048** — claims "Address: dropped — Person already carries rich
  town/post_code/country." **False:** those fields live on `Guest`
  (`reservations/models/guest.py:29-31`); `Person` has only `address_line_1/2`.
  They arrive only with GAP-045 phase-2. Correct the ticket; the role-enum
  reconciliation half is valid.
- **Q-008** — premise is half-built-already. A full TOTP `TwoFactorService`
  (enroll/challenge/verify/disable + admin reset) exists
  (`accounts/services/two_factor.py`, 4 endpoints). Re-scope from "implement 2FA"
  to the two genuinely-open sub-questions: **enforcement policy** (who is forced)
  and **refund step-up** (no fresh-TOTP gate on refund execute — `grep payments/`
  empty).
- **GAP-045** — should be **PARTIAL, not "uncoded."** Phase-1a landed 2026-06-18:
  Contact→Person rename (`accounts/models/person.py:19`, migration `0006`) +
  supply-side and agent FKs repointed to `accounts.Person`. The hard part is
  entirely undone: `reservations.Guest` survives as a separate model with its own
  merge/anonymize/address fields, and `Enquiry/Quotation/Booking.guest` still
  point at it. Update the ticket to reflect phase-1a; the Guest fold-in
  (data migrate, unify status/merge/address, retire Guest) is the remaining 2/3.
  Knock-on: **GAP-046** can trim its agent-FK-repoint step (already done by the
  rename).

## Housekeeping — stale numbers/paths inside valid tickets (4)

- **SMELL-010** — the banned-import smell **spread** since the ticket:
  `reservations/services/charges.py:23` now also imports DRF `ValidationError`
  (ticket named only `quotations.py`). Update the file list; the proposed ruff
  `TID251` ban would have caught the regression.
- **SMELL-008** — adoption counts are stale (e.g. `log_operation` now in 4 files,
  not 1). Refresh, then make the actual CLAUDE.md mandatory-vs-aspirational call —
  that decision still hasn't landed.
- **SMELL-011** — a pricing `assert_max_queries` now exists but pins the **engine
  quote path** (`pricing/tests/test_engine.py:982`), not a **list endpoint** as
  the acceptance requires; `accounts` still has zero pins. Tighten the wording so
  it isn't mis-read as resolved.
- **GAP-013** — file paths are stale (`QuoteCart*` → `QuoteShortlist*` after the
  rename); the UX items themselves (#1, #2, #4, #5, #7, #8) are still unbuilt.

## Dependency tangles that aren't written down

These are all VALID tickets; the risk is sequencing, not correctness.

- **Pricing-correctness cluster.** `BUG-009 → GAP-035` (net↔gross entry is
  genuinely BLOCKED until the engine honours `price_basis`); `Q-018` + `Q-022`
  share one carry-over rule (**copy the base band / season tier, not the
  reduction**); `Q-023` is partial-week composition. Owner answers are captured
  in several ticket bodies but **none are recorded in `10-decisions.md`** — the
  work is "write the decision + build," not "gather requirements."
- **Quote-builder rework chain.** `GAP-043` (multi-week range) → `GAP-044` (band
  fan-out — bands render *inside* 043's week boxes, hard one-way dep) → `GAP-013`
  polish **trails** (soft-blocked: building 013 against the surface 043/044 will
  rebuild risks rework). Both 043 and 044 **reverse a recorded decision**
  (`flexibility_days` rework; `04-pricing.md` "no auto fan-out") — log the
  reversal in `10-decisions.md` first, per the project's "surface the
  disagreement" rule. `GAP-005 #9` is absorbed by 043/044 (close-by-delegation).
- **iCal freshness/source cluster.** `GAP-011` residual (awareness digest) +
  `GAP-033` (last-confirmed timestamp) + `GAP-034` (source badge + calendar link)
  all share the same missing seam — `PropertyCalendarFeed` is in **zero**
  serializers/views/FE today. Land them together; never serialize the feed `url`.
- **People-model chain.** `GAP-045` (foundational, now half-done) blocks
  046/047/048/042/040/041. Separately, **GAP-029 is a live bug** — company-only
  contacts still 400 (`person.py:27-28` no `blank=True`) and the rename did *not*
  fix it. Decide whether to unblock GAP-029 standalone rather than wait on the
  months-out Organisation entity.

## Stale cross-refs to prune

- **Q-017** cites FG-013 as a "coordinate" dep — FG-013 is now in `done/`.
- **Q-021** cites GAP-022 as the "cheapest moment" to seed feature taxonomy —
  GAP-022 has landed, so seed the group defaults + curate the taxonomy **now**.

## Open product decisions (the real residue — need the user)

Verified genuinely-open against code/specs (not answered-but-unfiled):

- **BUG-008** — `DamageClaim` in v1? (blocks the SD damage slice)
- **Q-006** — owner statement cadence + delivery (**zero code**; top blocker, Workflow 18)
- **Q-007** — concierge supplier directory shape (no `Supplier` entity)
- **Q-010** — guest-data auto-anonymise cadence (manual `anonymize` already built)
- **Q-017** — comms: signals-only sink vs down-the-spine
- **Q-019** — structured room-attribute vocabulary (owns GAP-024's deferred posture)
- **Q-020** — description-section enum parity (verify vs the legacy prod snapshot)
- **Q-021** — seed group production defaults + curate feature taxonomy
- **Pricing model** — Q-018 / Q-022 / Q-023 / GAP-037 (decide together; see tangle above)

## Cheapest first commits

1. **FG-009** → move to `done/` (already shipped; doc-only).
2. **SMELL-013** + **SMELL-012** doc pass — de-facto "one aggregate per file"
   note in `django_res/CLAUDE.md:279` + move `refunds_for_booking` into
   `payments/views/`.
3. **SMELL-010** — add the ruff banned-import rule (catches the `charges.py`
   regression) + update the ticket's file list.

Three low-risk, no-design-dependency commits.
